version: 2

jobs:
  toxify:
    docker:
      - image: python:3.6
    steps:
      - checkout
      - run: pip install tox && tox
      - run: pip install codecov && codecov -t $CODECOV_TOKEN

workflows:
  version: 2
  kv_recursive_test:
    jobs:
      - toxify
